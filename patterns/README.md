# Patterns

This directory contains reusable operating patterns.

Examples:
- allowlist-based mutation
- validation before promotion
- read-only versus write actions
- cron versus heartbeat decision rules
